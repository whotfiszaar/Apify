{
  "buildCommand": "mkdir -p dist-static && cp landing.html dist-static/index.html",
  "outputDirectory": "dist-static",
  "framework": null,
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
